{% extends 'base.html.twig' %}

{% block title %}{{ "cart.update" | trans }}{% endblock %}

{% block body %}
    <h1>{{ "cart.update" | trans }}</h1>

    {{ include('cart/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('cart_index') }}">{{ "Back to list" | trans }}</a>

    {{ include('cart/_delete_form.html.twig') }}
{% endblock %}
