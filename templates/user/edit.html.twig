{% extends 'base.html.twig' %}

{% block title %}{{ "user.update" | trans }}{% endblock %}

{% block body %}
    <h1>{{ "user.update" | trans }}</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update' | trans }) }}

    <a href="{{ path('user_index') }}">{{ "Back to list" | trans }}</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
